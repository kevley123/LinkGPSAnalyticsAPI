Django>=5.2
djangorestframework>=3.15
django-cors-headers>=4.3
psycopg2-binary>=2.9
django-environ>=0.11

# ML Core
pandas>=2.0
numpy>=1.26
scikit-learn>=1.4
joblib>=1.3

# Geometry / spatial helpers (already handled by PostGIS via psycopg2)
scipy>=1.12         # for distance calculations

# Background tasks
celery>=5.3
redis>=5.0          # Celery broker + result backend
django-celery-beat>=2.6   # periodic tasks via DB

# Utilities
python-dotenv>=1.0  # .env loading
h3>=3.7.6           # Uber H3 Hexagonal clustering
